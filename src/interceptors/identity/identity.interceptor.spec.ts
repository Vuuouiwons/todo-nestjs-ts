import { IdentityInterceptor } from './identity.interceptor';

describe('IdentityInterceptor', () => {
  it('should be defined', () => {
    expect(new IdentityInterceptor()).toBeDefined();
  });
});
